class Kripke:
    def __init__(self, states, initial, transitions, labels):
        self.states = states
        self.initial = initial
        self.transitions = transitions
        self.labels = labels